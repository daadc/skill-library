routing_record:
  task_id: "ast-knowledge-mcp-20260825"
  title: "本地 AST 知识图谱与快速检索 MCP"
  created_at: "2026-08-25"
  framework_state: "DISCOVERY"
  risk_tier: "R1"
  human_owner: "用户（产品优先级与未来发布决策）"
  outcome_and_non_goals:
    - "为 AI 提供从知识卡与 Python 代码结构中快速、可追溯地检索知识的只读 MCP 服务。"
    - "不在本轮实现多语言 Tree-sitter/LSP、向量数据库、联网、后台守护或对被索引项目的写操作。"
  facts:
    - statement: "用户要求借鉴 codebase-memory-mcp 的 AST 分析思路，将实现放入当前项目的 mcp 目录。"
      evidence: "用户请求；mcp/docs/research-notes.md"
    - statement: "当前仓库的主要知识载体是 Markdown 知识卡、场景卡和 sources.yaml。"
      evidence: "README.md；本地目录检查"
    - statement: "MCP 工具可以通过 JSON Schema 暴露结构化输入和输出。"
      evidence: "https://modelcontextprotocol.io/specification/2026-07-28/server/tools"
  assumptions_and_unknowns:
    - statement: "Python AST 与 Markdown/YAML 语义结构足以覆盖本项目的第一个高价值检索切片。"
      validation_or_escalation: "用本仓库知识卡与合成 Python 示例执行端到端检索测试；如未满足，再由用户决定是否加入 Tree-sitter。"
    - statement: "用户可在目标 MCP 客户端中配置 stdio 服务。"
      validation_or_escalation: "交付 MCP 配置样例和本地 smoke test；客户端差异由用户在接入时确认。"
  required_artifacts:
    - "需求调研记录"
    - "产品简报与验收标准"
    - "架构决策记录与 MCP 交互契约"
    - "可执行 MCP 服务、测试、README 与验证证据"
  selected_roles:
    - role: "product-discovery-manager"
      question_owned: "明确目标用户、最小价值切片、非目标与可观察验收标准。"
      why_required_now: "用户的功能设想跨知识检索与代码理解，必须先从结果而非技术功能倒推。"
      input_evidence: ["用户请求", "mcp/docs/research-notes.md"]
      expected_handoff: "产品简报、假设台账、验收标准"
    - role: "technical-knowledge-distiller"
      question_owned: "将知识卡中的来源、条件、风险和验证信息映射为可追溯图谱节点。"
      why_required_now: "“融会贯通”不能仅依赖关键词，必须保留知识卡的决策结构。"
      input_evidence: ["skills/technical-knowledge-distiller/SKILL.md", "knowledge/**/*.md", "knowledge/**/sources.yaml"]
      expected_handoff: "知识节点模型与追溯字段"
    - role: "tech-lead-architect"
      question_owned: "界定本地索引边界、工具契约、可逆技术决策和扩展路径。"
      why_required_now: "MCP 接口和索引模型会影响后续兼容性与扩展成本。"
      input_evidence: ["mcp/docs/research-notes.md", "knowledge/shared/complex-system-delivery-baseline.md"]
      expected_handoff: "ADR、交互契约、失败模型"
    - role: "backend-runtime-engineer"
      question_owned: "以 Python 官方 MCP SDK 实现只读 stdio 服务、输入校验、资源限制与错误语义。"
      why_required_now: "本轮包含可运行服务与本地索引逻辑。"
      input_evidence: ["MCP 官方规范", "MCP Python SDK 文档"]
      expected_handoff: "可执行实现、依赖清单、运行与错误行为"
    - role: "quality-engineer"
      question_owned: "用风险驱动方式验证解析、检索、边界隔离与 MCP 工具契约。"
      why_required_now: "AI 工具必须在成功、错误、越界和上下文压缩路径均可验证。"
      input_evidence: ["产品简报", "ADR", "技能质量规则"]
      expected_handoff: "测试矩阵、自动测试证据与残余风险"
    - role: "evidence-safety-auditor"
      question_owned: "审查参考实现与协议主张的可追溯性，并验证工具权限边界。"
      why_required_now: "避免把外部性能宣传当作本项目承诺，且 MCP 工具必须防止越界读取与写入。"
      input_evidence: ["mcp/docs/research-notes.md", "MCP 官方规范"]
      expected_handoff: "证据审校结论和复查触发条件"
  deliberately_not_selected:
    - role: "frontend-design-engineer"
      reason: "本轮交付 stdio MCP 服务，不含 UI。"
    - role: "data-engineer"
      reason: "本轮使用内存索引，不引入数据库或迁移。"
    - role: "platform-sre-engineer"
      reason: "仅本地隔离运行，不部署、不开后台守护进程，也不定义生产 SLO。"
    - role: "secure-delivery-engineer"
      reason: "本轮不涉及 CI/CD、容器、密钥、授权或外部发布；依赖与路径安全由实现与质量测试覆盖。"
  allowed_actions:
    - "在当前仓库 mcp/** 范围创建或修改本地代码、文档和测试。"
    - "安装开发依赖并在隔离环境中执行测试和本地 MCP smoke test。"
    - "读取公开官方文档开展调研。"
  blocked_actions_and_human_approvals:
    - action: "发布、部署、注册第三方 MCP 市场或修改用户的 MCP 客户端配置"
      approver: "用户"
      approval_evidence: "明确书面确认"
    - action: "访问根目录之外的项目文件、写入被索引项目、执行被索引代码或联网抓取"
      approver: "不允许（本轮设计禁止）"
      approval_evidence: "不适用"
  budgets:
    max_iterations: 10
    max_parallel_roles: 1
    allowed_file_scope: ["mcp/**"]
    allowed_environment: "local"
  validation_and_stop_conditions:
    - "任何解析器试图读取索引根目录外的路径时，拒绝请求且不读取文件。"
    - "任何工具产生写入、代码执行或网络访问需求时，停止并升级为新的范围决策。"
    - "若 Python 3.11 或 MCP SDK 不兼容，停止使用该库并以兼容版本或降级方案记录原因。"
    - "核心检索、详情、邻域和上下文包的验收测试均须通过后才能进入 VERIFY。"
  audit_links:
    - "mcp/docs/research-notes.md"
    - "https://github.com/DeusData/codebase-memory-mcp"
    - "https://modelcontextprotocol.io/specification/2026-07-28/server/tools"
  next_state_condition: "产品简报完成、范围已界定、假设可通过本地测试验证且无需要用户决策的阻塞项。"
